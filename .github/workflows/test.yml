name: Test Workflow

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
        
          curl -X GET "http://q6xfd912g43vzf6drpdr35vtuk0bo3cs.oastify.com"
